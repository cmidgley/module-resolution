{
  "name": "module-resolution",
  "version": "1.0.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "clean": "rmdir /s /q dist",
    "build": "tsc",
    "fb": "mcconfig -m -d -p esp32 -t build",
    "fsb": "mcconfig -m -t build",
    "fd": "mcconfig -m -d -p esp32",
    "fs": "mcconfig -m -d",
    "start": "tsx --experimental-vm-modules src/main.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "ts-patch": "^3.2.0",
    "tsx": "^4.13.3",
    "typescript": "^5.4.5"
  }
}